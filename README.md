# Amazon ML Challenge 2026 — Business Entity Resolution

This is our team's pipeline for matching Source 1 businesses to their records in Sources 2 and 3.
Read `CLAUDE.md` first for the project brief, rules, architecture and data contracts, then `TEAM_PLAN.md` for owners and the timeline.
The code is in `code/business_entity_resolution/`; EDA findings are in `notes/DATA_CONTEXT.md`.
**The data is not in git.** Unzip the official `student_resource` bundle locally into `student_resource/` at the repo root.
