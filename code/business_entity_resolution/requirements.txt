# Python 3.12.3. Exact versions from the team .venv (pip freeze, direct deps only).
numpy==2.5.3
pandas==3.0.6
pyarrow==25.0.1
rapidfuzz==3.14.6

# Planned — pin the exact version when first added:
# polars==
# duckdb==
# scikit-learn==
# lightgbm==
# anyascii==
# pyyaml==
