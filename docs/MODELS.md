# Models

Every pretrained or trained model we use: it must be MIT or Apache-2.0 licensed and ≤ 8B parameters (CLAUDE.md §2).
Record the exact HF revision (commit hash) when you download it.

| model | license | HF revision | params | used for |
|---|---|---|---|---|
